<ul class="thumbs inline">
  <%- PortfolioPiece.order('created_at asc').each do |piece| %>
    <li><%= link_to image_tag(piece.thumbnail_url), piece, :title => piece.title, :class => 'hover-select', :"data-piece" => piece.slug %>
  <%- end %>
</ul>
